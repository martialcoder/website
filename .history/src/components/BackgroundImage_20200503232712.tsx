import styled from "@emotion/styled";

const BackgroundImage = styled.div`
  position: relative;
  background-image: url(${SOURCES.hill});
  width: 100%;
  height: 100%;
`;

export default BackgroundImage;
